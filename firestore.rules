rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only your logged-in account can read/write, now that login is wired up.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
